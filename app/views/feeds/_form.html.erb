<div class="edit-user-main">
  <%= form_with(model: feed, local: true, url:choose_new_or_edit) do |form| %>
    <% if feed.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(feed.errors.count, "error") %> prohibited this feed from being saved:</h2>

        <ul>
        <% feed.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <div class="field">
      <%= form.label :image %>
      <p id="edit-image"><%= image_tag(@feed.image.url) if @feed.image && @feed.image.url %></p>
      <%= form.file_field :image %>
      <%= form.hidden_field :image_cache %>
    </div>

    <div class="field">
      <%= form.label :content %>
      <p id="feed-content"><%= form.text_area :content %></p>
    </div>

    <div class="actions">
      <%= form.submit value="投稿する" %>
    </div>
  <% end %>

</div>
